# ─── Asosiy ───────────────────────────────────────────────────────
Django==5.2.6
python-docx==1.2.0

# python-docx va Django bog'liqliklari
asgiref==3.11.1
lxml==6.1.1
sqlparse==0.5.5
typing_extensions==4.15.0
tzdata==2026.2

# ─── Ishga tushirish (production) ─────────────────────────────────
# Statik fayllarni Django o'zi uzatadi — alohida nginx sozlash shart emas.
whitenoise==6.7.0

# WSGI server. Linux/macOS uchun gunicorn:
gunicorn==23.0.0
# Windows uchun gunicorn ishlamaydi, o'rniga waitress:
#   pip install waitress

#   waitress-serve --port=8000 config.wsgi:application
